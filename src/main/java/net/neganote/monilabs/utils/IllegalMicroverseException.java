package net.neganote.monilabs.utils;

public class IllegalMicroverseException extends UnknownError {

    public IllegalMicroverseException(String errorMessage) {
        super(errorMessage);
    }
}
